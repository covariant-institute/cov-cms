import React, { FC } from 'react';

export const Home: FC = () => {
  return (
    <div style={{
      height: '100vh',
      display: 'flex',
      alignItems: 'center',
      fontSize: 24,
      justifyContent: 'center',
    }}>
      <span>Welcome to Cov Club</span>
    </div>
  );
};
